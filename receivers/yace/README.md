<!-- status autogenerated section -->
# YACE Receiver

Receives AWS CloudWatch metrics by running the Prometheus
[yet-another-cloudwatch-exporter](https://github.com/prometheus-community/yet-another-cloudwatch-exporter)
as an OpenTelemetry Collector receiver.

| Status        |           |
| ------------- |-----------|
| Stability     | [development]: metrics   |
| Distributions | [prometheus] |
| Issues        | [![Open issues](https://img.shields.io/github/issues-search/prometheus/prometheus-opentelemetry-collector?query=is%3Aissue%20is%3Aopen%20label%3Areceiver%2Fyace%20&label=open&color=orange&logo=opentelemetry)](https://github.com/prometheus/prometheus-opentelemetry-collector/issues?q=is%3Aopen+is%3Aissue+label%3Areceiver%2Fyace) [![Closed issues](https://img.shields.io/github/issues-search/prometheus/prometheus-opentelemetry-collector?query=is%3Aissue%20is%3Aclosed%20label%3Areceiver%2Fyace%20&label=closed&color=blue&logo=opentelemetry)](https://github.com/prometheus/prometheus-opentelemetry-collector/issues?q=is%3Aclosed+is%3Aissue+label%3Areceiver%2Fyace) |
| [Code Owners](https://github.com/open-telemetry/opentelemetry-collector-contrib/blob/main/CONTRIBUTING.md#becoming-a-code-owner)    | [@ArthurSens](https://www.github.com/ArthurSens), [@tristanburguess](https://www.github.com/tristanburguess), [@andriikushch](https://www.github.com/andriikushch) |

[development]: https://github.com/open-telemetry/opentelemetry-collector/blob/main/docs/component-stability.md#development
[prometheus]: 
<!-- end autogenerated section -->

## Configuration

`scrape_interval` controls how often the embedded exporter is polled (default
`30s`). `exporter_config` is decoded onto the upstream
`yet-another-cloudwatch-exporter` (YACE) `config.Config`. The CloudWatch
discovery/static jobs are **not** configured here — they live in a separate YACE
config file referenced by `scrape_config_file`.

```yaml
receivers:
  yace_exporter:
    scrape_interval: 60s
    exporter_config:
      scrape_config_file: /etc/otel/yace-config.yaml   # required
```

| `exporter_config` key | Description |
| --- | --- |
| `scrape_config_file` | _(required)_ Path to the YACE config file (discovery/static jobs, regions, roles). |
| `metrics_per_query` | Max CloudWatch metrics per `GetMetricData` call. |
| `labels_snake_case` | Emit labels in snake_case. |
| `tagging_api_concurrency` | Concurrency for the resource-tagging API. |
| `cloudwatch_concurrency` | CloudWatch API concurrency (`single_limit`, `per_api_limit_enabled`, `list_metrics`, `get_metric_data`, `get_metric_statistics`). |
| `feature_flags` | YACE feature flags (e.g. `aws-sdk-v2`). |
| `fips_enabled` | Use FIPS AWS endpoints. |

`scrape_config_file` takes a standard YACE config — the same format the
standalone exporter's `--config.file` accepts. See YACE's
[configuration docs](https://github.com/prometheus-community/yet-another-cloudwatch-exporter/blob/master/docs/configuration.md)
for the full schema (discovery/static jobs, regions, roles, tags); a minimal example:

```yaml
# yace-config.yaml
apiVersion: v1alpha1
sts-region: us-east-2
discovery:
  jobs:
    - type: AWS/EC2
      regions: [us-east-2]
      metrics:
        - name: CPUUtilization
          statistics: [Average]
          period: 300
          length: 300
```
